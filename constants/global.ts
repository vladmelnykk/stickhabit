import { Dimensions } from 'react-native'

const { width: WINDOW_WIDTH, height: WINDOW_HEIGHT } = Dimensions.get('window')

const CONTAINER_PADDING = 20

export { CONTAINER_PADDING, WINDOW_HEIGHT, WINDOW_WIDTH }
